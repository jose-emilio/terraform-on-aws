# Backend config para Lab-26 en AWS real.
# Usa el bucket creado en el Lab-02 (terraform-state-labs-<ACCOUNT_ID>).
# Úsalo así:
#   terraform init -backend-config=aws.s3.tfbackend -backend-config="bucket=terraform-state-labs-<ACCOUNT_ID>"
key            = "lab-26/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
use_lockfile   = true
